<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <title>Liste presence</title>
    <style>
        h2{
            text-align: center;
            font-weight: bold;
            padding-top: 1em;
            padding-bottom: 1em;
        }
        .table{
            padding-top: 2em;
        }
    </style>
</head>
<body>
    <h2><%= title  %> </h2>
    <div class="container">

        <table class="table table-dark table-striped">
            <thead>
                <tr>
                    <th>Numero</th>
                    <th>Sceance</th>
                    <th>Prenom</th>
                    <th>Nom</th>
                    <th>Nombre Connexion</th>
                    <th>Nombre Deconnexion</th>
                    <th>Email</th>
                    <th>Region</th>
                </tr>
            </thead>
           <tbody>
               <% if(resultat.length != 0){
                   for (data of resultat) {%>
                <tr>
                    <td><%=data.idEtudiant %> </td>
                    <td><%=data.numeroSeance %> </td>
                    <td><%=data.prenom %> </td>
                    <td><%=data.nom %> </td>
                    <td><%=data.nombreConnexion %></td>
                    <td><%=data.nombreDeconnexion %></td>
                    <td><%=data.email %></td>
                    <td><%=data.region %></td>
                </tr>     
                <%}%>
                <% } else{%>        
                    <tr>
                        <td>No data found</td>
                        <td>No data found</td>
                        <td>No data found</td>
                        <td>No data found</td>
                        <td>No data found</td>
                        <td>No data found</td>
                        <td>No data found</td>
                    </tr>
                <% } %>    
           </tbody>
        </table>
    </div>
</body>
</html>